# =====================================================================
#  i-Tafaray — lancement local en un service
#  Démarrer :  docker compose up -d --build
#  Ouvrir   :  http://localhost:3838
#  Arrêter  :  docker compose down
# =====================================================================

services:
  app:
    build: .
    image: itafaray:latest
    container_name: itafaray
    restart: unless-stopped
    ports:
      - "3838:3838"
