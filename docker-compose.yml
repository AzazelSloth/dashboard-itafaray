services:
  app:
    image: ${APP_IMAGE:-itafaray-poc:pre-prod}
    build: .
    container_name: itafaray-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3838:3838"
    volumes:
      - /var/lib/itafaray-dashboard/xroad:/data/xroad
