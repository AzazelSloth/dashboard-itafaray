name: Pre-prod CI/CD

on:
  workflow_call:
    inputs:
      target_environment:
        required: true
        type: string

permissions:
  contents: read
  packages: write

jobs:
  run:
    uses: ./.github/workflows/_deploy-template.yml
    with:
      target_environment: ${{ inputs.target_environment }}
    secrets: inherit